# Development Authentication Settings
# Set DEV_AUTH=1 to enable internal dev auth mode without external OAuth providers
DEV_AUTH=1

# Cookie Secret for Session Management
# Generate a secure random string for production
COOKIE_SECRET=your-secure-random-secret-min-32-chars

# Base URL
# Set this to your application's base URL
BASE_URL=http://localhost:3000

# Clerk Authentication (Production Only - Optional in Dev Mode)
# Get these from https://clerk.com dashboard
# Leave empty or use placeholder values in dev mode when DEV_AUTH=1
CLERK_PUBLISHABLE_KEY=pk_test_xxxxx
CLERK_SECRET_KEY=sk_test_xxxxx

# Database
# Cloudflare D1 is configured in wrangler.jsonc
# For local development, wrangler uses local SQLite automatically with --local flag

# Cloudflare API Token (Production Deployment Only)
# Get from https://dash.cloudflare.com/profile/api-tokens
# CLOUDFLARE_API_TOKEN=your_api_token_here
